
// compiling less files
gulp.task('{{engine.task}}', function () {
  return gulp.src('{{source}}/**/**/*.less')
    .pipe(gulpLess({
      paths: ['{{source}}/less/includes']
    }))
    .pipe(gulp.dest('{{build}}/'))
    .pipe(gulpPrint());
});
